#!/bin/bash
source ~/.bashrc

alias e="vcp edit" # open vscode
alias w="vcp watch" # open vscode + compile debug + run + recompile + run tests

alias c="vcp compile" # compile release
alias cd="vcp compile debug" # compile debug

alias r="vcp run" # compile release + run
alias t="vcp test" # run tests

alias s="vcp submit" # copy source to clipboard

#

alias aa="vcp a"
alias bb="vcp b"
alias cc="vcp c"
alias dd="vcp d"
alias ee="vcp e"
alias ff="vcp f"
alias gg="vcp g"
alias hh="vcp h"
